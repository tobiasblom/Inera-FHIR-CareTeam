name: Build and publish IG

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'support/**'
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    name: Build IG
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Determine deploy path and validate tag
        id: meta
        run: |
          REF="${{ github.ref }}"
          BRANCH="${{ github.ref_name }}"

          if [[ "$REF" == refs/tags/v* ]]; then
            VERSION="${BRANCH#v}"
            CONFIG_VERSION=$(grep '^version:' sushi-config.yaml | awk '{print $2}' | tr -d '"' | tr -d "'")
            if [ "$VERSION" != "$CONFIG_VERSION" ]; then
              echo "::error::Tag ${BRANCH} (${VERSION}) does not match sushi-config.yaml version (${CONFIG_VERSION}). Update sushi-config.yaml before tagging."
              exit 1
            fi
            echo "deploy_path=${VERSION}"    >> $GITHUB_OUTPUT
            echo "is_release=true"           >> $GITHUB_OUTPUT
            echo "version=${VERSION}"        >> $GITHUB_OUTPUT
          elif [[ "$BRANCH" == "main" ]]; then
            echo "deploy_path="            >> $GITHUB_OUTPUT
            echo "is_release=false"        >> $GITHUB_OUTPUT
          elif [[ "$BRANCH" == "develop" ]]; then
            echo "deploy_path=branches/develop" >> $GITHUB_OUTPUT
            echo "is_release=false"             >> $GITHUB_OUTPUT
          else
            SLUG=$(echo "$BRANCH" | sed 's|[^a-zA-Z0-9]|-|g' | sed 's|-\+|-|g' | sed 's|^-||;s|-$||')
            echo "deploy_path=branches/${SLUG}" >> $GITHUB_OUTPUT
            echo "is_release=false"             >> $GITHUB_OUTPUT
          fi

          echo "ig_name=${{ github.event.repository.name }}" >> $GITHUB_OUTPUT

          # Extract the last path segment of canonical as the fhir-portal slug.
          # e.g. https://fhir.inera.se/ig/template → template
          CANONICAL_SLUG=$(python3 -c "import json; d=json.load(open('package-list.json')); print(d['canonical'].rstrip('/').split('/')[-1])")
          echo "ig_slug=${CANONICAL_SLUG}" >> $GITHUB_OUTPUT

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Ruby and Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'

      - name: Install Jekyll
        run: gem install jekyll

      - name: Install SUSHI
        run: npm install -g fsh-sushi

      - name: Restore FHIR package cache
        uses: actions/cache@v4
        with:
          path: ~/.fhir/packages
          key: fhir-packages-${{ hashFiles('sushi-config.yaml') }}
          restore-keys: fhir-packages-

      - name: Restore IG Publisher cache
        uses: actions/cache@v4
        with:
          path: input-cache/publisher.jar
          key: ig-publisher-${{ hashFiles('ig.ini') }}

      - name: Download IG Publisher
        run: |
          mkdir -p input-cache
          if [ ! -f input-cache/publisher.jar ]; then
            curl -sL https://github.com/HL7/fhir-ig-publisher/releases/latest/download/publisher.jar \
              -o input-cache/publisher.jar
          fi

      - name: Run SUSHI
        run: sushi .

      - name: Build IG
        run: |
          build() {
            java -jar input-cache/publisher.jar -ig ig.ini "$@"
          }
          if build -tx https://tx.fhir.org/r4; then
            echo "Build succeeded with terminology server."
          else
            echo "TX attempt 1 failed, retrying in 30s..."
            sleep 30
            if build -tx https://tx.fhir.org/r4; then
              echo "Build succeeded on retry."
            else
              echo "TX server unavailable — building without terminology validation."
              build -tx n/a
            fi
          fi

      - name: Read IG metadata from build output
        run: |
          MANIFEST=output/package.manifest.json
          PKG_ID=$(jq -r '.name' "$MANIFEST")
          echo "IG_VERSION=$(jq -r '.version' "$MANIFEST")" >> $GITHUB_ENV
          IG_FILE="output/ImplementationGuide-${PKG_ID}.json"
          echo "IG_TITLE=$(jq -r '.title' "$IG_FILE")"       >> $GITHUB_ENV
          echo "IG_DESCRIPTION=$(jq -r '.description' "$IG_FILE")" >> $GITHUB_ENV

      - name: Update package-list.json for tagged release
        if: steps.meta.outputs.is_release == 'true'
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          export DATE=$(date +%Y-%m-%d)
          python3 - <<'PYEOF'
          import json, os
          v    = os.environ['VERSION']
          date = os.environ['DATE']

          with open('package-list.json') as f:
              pl = json.load(f)

          # Use the canonical URL from package-list.json as the base —
          # avoids mismatch between repo name and canonical slug.
          base = pl['canonical'].rstrip('/')

          pl['list'] = [e for e in pl['list'] if e.get('version') != v]
          pl['list'].append({
              "version": v,
              "desc": f"Release {v} ({date})",
              "path": f"{base}/{v}",
              "status": "draft",
              "sequence": "STU 1",
              "fhirversion": ["4.0.1"],
              "current": False
          })

          with open('package-list.json', 'w') as f:
              json.dump(pl, f, indent=2)
              f.write('\n')
          PYEOF

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package-list.json
          git commit -m "chore: add package-list entry for v${{ steps.meta.outputs.version }}"
          git push origin HEAD:main

      - name: Deploy to GitHub Pages (gh-pages branch)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output
          destination_dir: ${{ steps.meta.outputs.deploy_path }}
          keep_files: true
          commit_message: "deploy: ${{ github.ref_name }} → ${{ steps.meta.outputs.deploy_path }}"

      - name: Prepare IG root files for history page
        if: steps.meta.outputs.is_release == 'true'
        run: |
          mkdir -p /tmp/ig-root
          cp package-list.json /tmp/ig-root/package-list.json

          # downloads.md links to <canonical>/package.tgz, so the package has
          # to exist at the IG root and not only under the version directory.
          # This is the "latest release" copy of the package.
          if [ -f output/package.tgz ]; then
            cp output/package.tgz /tmp/ig-root/package.tgz
          else
            echo "::warning::output/package.tgz missing — the download link at the IG root will not resolve"
          fi

          # fhir2.base.template emits pages under a language directory
          # (output/sv/...), so look there as well as at the output root.
          HISTORY=""
          for c in output/history.html output/*/history.html; do
            if [ -f "$c" ]; then HISTORY="$c"; break; fi
          done

          if [ -n "$HISTORY" ]; then
            echo "Using history.html from ${HISTORY}"
            cp "$HISTORY" /tmp/ig-root/history.html
            cp "$HISTORY" /tmp/ig-root/index.html
          else
            echo "history.html not generated — building fallback index from package-list.json"
            python3 - <<'PYEOF'
          import json
          with open('package-list.json') as f:
              pl = json.load(f)
          title = pl.get('title', pl.get('package-id', ''))
          canonical = pl['canonical'].rstrip('/')
          versions = [e for e in pl.get('list', []) if e.get('version') != 'current']
          rows = ''.join(
              f'<tr><td><a href="{e["path"]}/">{e["version"]}</a></td>'
              f'<td>{e.get("desc","")}</td>'
              f'<td>{e.get("status","")}</td></tr>'
              for e in reversed(versions)
          )
          html = f"""<!DOCTYPE html>
          <html lang="sv"><head><meta charset="utf-8">
          <title>{title} – versioner</title>
          <style>body{{font-family:sans-serif;max-width:800px;margin:2rem auto;padding:0 1rem}}
          table{{border-collapse:collapse;width:100%}}th,td{{text-align:left;padding:.5rem;border-bottom:1px solid #ddd}}</style>
          </head><body>
          <h1>{title}</h1>
          <p>Canonical: <a href="{canonical}">{canonical}</a></p>
          <table><thead><tr><th>Version</th><th>Beskrivning</th><th>Status</th></tr></thead>
          <tbody>{rows}</tbody></table>
          </body></html>"""
          # Written to both names: the publisher stamps a link to
          # <canonical>/history.html into every page, while <canonical>/ itself
          # needs an index. Writing only one of them leaves the other a 404.
          for name in ('index.html', 'history.html'):
              with open(f'/tmp/ig-root/{name}', 'w') as f:
                  f.write(html)
          PYEOF
          fi

      - name: Deploy history.html to gh-pages root
        if: steps.meta.outputs.is_release == 'true'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: /tmp/ig-root
          destination_dir: ""
          keep_files: true
          commit_message: "deploy: history.html + package-list.json at root for v${{ steps.meta.outputs.version }}"

      # ---------------------------------------------------------------
      # fhir-portal integration (optional — release builds only)
      # Activate by adding FHIR_PORTAL_TOKEN as a repository secret:
      #   Settings → Secrets and variables → Actions → New repository secret
      # A fine-grained PAT with Contents: Read and write on inera-ab/fhir-portal.
      # IG metadata is read from build output (package.manifest.json / ImplementationGuide-*.json).
      # ---------------------------------------------------------------
      - name: Publish IG to fhir-portal
        if: steps.meta.outputs.is_release == 'true' && env.FHIR_PORTAL_TOKEN != ''
        env:
          FHIR_PORTAL_TOKEN: ${{ secrets.FHIR_PORTAL_TOKEN }}
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.FHIR_PORTAL_TOKEN }}
          external_repository: inera-ab/fhir-portal
          publish_branch: gh-pages
          publish_dir: ./output
          destination_dir: ig/${{ steps.meta.outputs.ig_slug }}/${{ env.IG_VERSION }}
          keep_files: true
          commit_message: "deploy: ${{ steps.meta.outputs.ig_slug }} v${{ env.IG_VERSION }}"

      - name: Deploy history.html to fhir-portal IG root
        if: steps.meta.outputs.is_release == 'true' && env.FHIR_PORTAL_TOKEN != ''
        env:
          FHIR_PORTAL_TOKEN: ${{ secrets.FHIR_PORTAL_TOKEN }}
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.FHIR_PORTAL_TOKEN }}
          external_repository: inera-ab/fhir-portal
          publish_branch: gh-pages
          publish_dir: /tmp/ig-root
          destination_dir: ig/${{ steps.meta.outputs.ig_slug }}
          keep_files: true
          commit_message: "deploy: history.html at ig/${{ steps.meta.outputs.ig_slug }} for v${{ env.IG_VERSION }}"

      - name: Update IG registry on fhir-portal
        if: env.FHIR_PORTAL_TOKEN != ''
        env:
          FHIR_PORTAL_TOKEN: ${{ secrets.FHIR_PORTAL_TOKEN }}
          GH_TOKEN: ${{ secrets.FHIR_PORTAL_TOKEN }}
        run: gh api repos/inera-ab/fhir-portal/dispatches --method POST -f event_type=ig-deployed -f "client_payload[ig_name]=${{ github.event.repository.name }}" -f "client_payload[ig_slug]=${{ steps.meta.outputs.ig_slug }}" -f "client_payload[ig_title]=${{ env.IG_TITLE }}" -f "client_payload[ig_description]=${{ env.IG_DESCRIPTION }}" -f "client_payload[ig_version]=${{ env.IG_VERSION }}" -f "client_payload[is_release]=${{ startsWith(github.ref, 'refs/tags/v') }}"
